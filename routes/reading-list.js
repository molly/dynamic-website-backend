const express = require('express');

const router = express.router();

router.get('/tags', async (req, res) => {
  const tags = {};
  const categories = ['blockchain', 'press', 'shortform'];
  for (const category of categories) {
  }
});
